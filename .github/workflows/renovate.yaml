# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/renovate.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
concurrency:
  cancel-in-progress: false
  group: renovate
jobs:
  renovate:
    if: |-
      github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issues' && github.event.sender.type == 'User' && github.event.issue.title == 'Dependency Dashboard') ||
      (github.event_name == 'pull_request' && github.event.sender.type == 'User' && startsWith(github.event.pull_request.head.ref, 'renovate/'))
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
    - uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24
      with:
        nix_path: nixpkgs=channel:nixos-unstable
    - env:
        LOG_LEVEL: debug
        RENOVATE_CUSTOM_ENV_VARIABLES: '{"GITHUB_TOKEN":"{{ secrets.GITHUB_TOKEN }}"}'
        RENOVATE_SECRETS: '{"GITHUB_TOKEN":"${{ secrets.GITHUB_TOKEN }}"}'
      name: Self-hosted Renovate
      uses: renovatebot/github-action@9fea9f0fbf80401026d11d03911d62b1f70fef1f
      with:
        configurationFile: .github/renovate-global-config.json
        docker-cmd-file: modules/repository/ci/renovate-docker-cmd.sh
        docker-user: root
        docker-volumes: /tmp:/tmp ; /nix:/nix
        renovate-version: 44.68.3
        token: ${{ secrets.RENOVATE_TOKEN }}
name: renovate
'on':
  issues:
    types:
    - edited
  pull_request:
    types:
    - edited
  schedule:
  - cron: 0 5 * * *
  workflow_dispatch: {}
